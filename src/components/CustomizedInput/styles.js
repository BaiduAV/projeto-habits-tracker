import styled from "styled-components";

export const Container = styled.div`
    margin-top: 0.2rem;

    div {
        color: var(--white);
        padding-bottom: 0.5rem;
        font-weight: bold;
    }

    span {
        color: var(--red)
    }

`